services:
  api:
    build: .
    image: happy-hours-krakow:local
    env_file:
      - .env
    ports:
      - "${PORT:-8000}:8000"
    restart: unless-stopped

  seed:
    image: happy-hours-krakow:local
    env_file:
      - .env
    profiles:
      - tools
    command: ["python", "seed_data.py"]

  pipeline:
    image: happy-hours-krakow:local
    env_file:
      - .env
    profiles:
      - tools
    command: ["python", "run_pipeline.py"]
